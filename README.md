# Kezia Lim — Personal Site

Next.js (App Router) + Tailwind CSS v4.

## Getting Started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Content

Page copy lives directly in the `app/**/page.tsx` files. Items still marked
`TODO` in the UI need real content from Kezia (client names, project write-ups,
Insights post bodies, screenshots).
